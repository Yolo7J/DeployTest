name: CI/CD ASP.NET MVC to SmarterASP

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    - name: Restore dependencies
      run: dotnet restore DeployTest/DeployTest.csproj

    - name: Build
      run: dotnet build DeployTest/DeployTest.csproj -c Release --no-restore

    - name: Run tests
      run: dotnet test DeployTest/DeployTest.csproj --no-build --verbosity normal

    - name: Publish
      run: dotnet publish DeployTest/DeployTest.csproj -c Release -o ./publish

    - name: Deploy to SmarterASP via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        local-dir: ./publish/
        server-dir: ${{ secrets.FTP_REMOTE_DIR }}
        dangerous-clean-slate: true
